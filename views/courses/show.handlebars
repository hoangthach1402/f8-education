<div class="mt-4">
    <div class="row">
        <div class="col-md-3">
            <button>Hoc ngay</button>
        </div>
        <div class="col-md-9">
            <h2>{{course.name}}</h2>
        <iframe width="852" height="479" src="https://www.youtube.com/embed/{{course.videoId}}" title="YouTube video player" frameborder="0" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture" allowfullscreen></iframe>
    <p>{{course.description}}</p>  
        </div>
    </div>
</div>

